print('nishanth')
print(192211678)
def fib(n, memo={}):
    if n in memo:
        return memo[n]
    if n <= 1:
        memo[n] = n
    else:
        memo[n] = fib(n - 1, memo) + fib(n - 2, memo)
    return memo[n]
n = int(input("Enter the value of n: "))
result = fib(n)
print(f"Fib({n}) = {result}")
